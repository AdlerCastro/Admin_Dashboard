import { Meta, StoryObj } from "@storybook/react";
import Login from ".";

export default {
    tilte: "Components/Templates/Login",
    component: Login
} as Meta<StoryObj>

type Story = StoryObj

export const Page: Story = {}