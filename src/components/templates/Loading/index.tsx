export default function LoadingPage() {
    return (
        <div className="flex w-full h-full items-center justify-center bg-zinc-900 text-zinc-50">
            <h1 className="font-semibold text-4xl animate-ping duration-1000">Carregando...</h1>
        </div>
    )
}