import { Meta, StoryObj } from "@storybook/react";
import UserProfilePage from ".";

export default {
    tilte: "Components/Templates/UserProfile",
    component: UserProfilePage
} as Meta<StoryObj>

type Story = StoryObj

export const Page: Story = {}