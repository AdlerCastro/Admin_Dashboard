import { Meta, StoryObj } from "@storybook/react";
import Hero from ".";

export default {
    tilte: "Components/Templates/Hero",
    component: Hero
} as Meta<StoryObj>

type Story = StoryObj

export const Page: Story = {}